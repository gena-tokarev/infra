[defaults]
host_key_checking = True
interpreter_python = auto_silent
local_tmp = ./.ansible-tmp
retry_files_enabled = False
roles_path = ./roles
stdout_callback = default

[ssh_connection]
# The deployment account uses password-based sudo. Pipelining can prevent
# Ansible from receiving the privilege-escalation prompt reliably.
pipelining = False
