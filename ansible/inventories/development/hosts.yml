all:
  hosts:
    cortex-vps:
      ansible_host: 167.233.59.107
      ansible_port: 22
      ansible_user: deploy
      # Ubuntu 25.10+ defaults to sudo-rs, whose PAM prompt is not yet
      # understood by Ansible's sudo become plugin. Use the supported classic
      # sudo implementation for Ansible without changing the host-wide default.
      ansible_become_exe: /usr/bin/sudo.ws
